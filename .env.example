# ==================== DATABASE ====================
# Supabase PostgreSQL Connection String (Recommended - FREE tier!)
# Get from: Supabase Dashboard → Settings → Database → Connection String → URI (Transaction mode)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres.abcdefghijk:YOUR_PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# Alternative: DigitalOcean Managed Database ($15/month)
# DATABASE_URL=postgresql://doadmin:PASSWORD@your-db-host.db.ondigitalocean.com:25060/emperion_knowledge_base?sslmode=require

# Alternative: Local PostgreSQL (development)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/emperion_knowledge_base

# ==================== SECURITY ====================
# CRITICAL: Change in production! Generate with: openssl rand -hex 32
MCP_SECRET_KEY=change-me-in-production-use-random-string-min-32-chars

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=https://claude.ai,http://localhost:3000

# ==================== RATE LIMITING ====================
RATE_LIMIT_PER_HOUR=100

# ==================== FILE PROCESSING ====================
MAX_FILE_SIZE_MB=10

# ==================== LOGGING ====================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ==================== SUPABASE SETUP NOTES ====================
# 1. Create project at https://supabase.com/dashboard
# 2. Go to Settings → Database
# 3. Scroll to "Connection string"
# 4. Select "URI" format
# 5. Choose "Transaction" mode (recommended for connection pooling)
# 6. Copy the connection string
# 7. Replace [YOUR-PASSWORD] with your database password
# 8. Paste as DATABASE_URL value above
#
# Free tier includes:
# - 500 MB database storage
# - Unlimited API requests
# - SSL/TLS encryption
# - Automatic backups
# - Connection pooling
