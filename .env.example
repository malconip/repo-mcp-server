# ==================== DATABASE ====================
# DigitalOcean Managed Database connection string
# Format: postgresql://username:password@host:port/database?sslmode=require
# DigitalOcean will auto-inject ${db.DATABASE_URL} in production
DATABASE_URL=postgresql://doadmin:CHANGE_ME@your-db-host.db.ondigitalocean.com:25060/emperion_knowledge_base?sslmode=require

# ==================== SECURITY ====================
# CRITICAL: Change in production! Generate with: openssl rand -hex 32
MCP_SECRET_KEY=change-me-in-production-use-random-string-min-32-chars

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=https://claude.ai,http://localhost:3000

# ==================== RATE LIMITING ====================
RATE_LIMIT_PER_HOUR=100

# ==================== FILE PROCESSING ====================
MAX_FILE_SIZE_MB=10

# ==================== LOGGING ====================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ==================== DIGITALOCEAN NOTES ====================
# 1. Create Managed PostgreSQL database first
# 2. Copy connection string from DigitalOcean dashboard
# 3. Add DATABASE_URL as encrypted secret in App Platform
# 4. Generate secure SECRET_KEY: openssl rand -hex 32
# 5. Add MCP_SECRET_KEY as encrypted secret in App Platform
